export default function Message(){
    // create the compnent that will contain all the
    // messages derived from messages state
    const Messages = () => {
        return <ul>
            <li></li>
        </ul>
    }
}